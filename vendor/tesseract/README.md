# Tesseract vendor bundle

`python src/packaging/build.py` populates this ignored directory from
`TESSERACT_ROOT` or a standard Windows Tesseract installation. The build
includes the runtime executable and DLLs plus English language data only.

